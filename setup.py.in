#!/usr/bin/env python

from distutils.core import setup
import os
import sys

print sys.argv[1]

setup(name = 'hydra-agent',
      license = 'Proprietary',
      version = '@VERSION@',
      author = "Whamcloud, Inc.",
      author_email = "info@whamcloud.com",
      description = 'The Whamcloud Lustre Monitoring and Adminisration Interface Agent', 
      long_description = 'This is the Whamcloud Monitoring and Adminstration Interface Agent',
      url = 'http://www.whamcloud.com/',
      packages = [''],
      scripts = [],
      )

#if os.path.exists('monitor/bin/hydra-monitor'):
#    os.unlink('monitor/bin/hydra-monitor')
